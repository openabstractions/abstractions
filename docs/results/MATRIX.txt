# MATRIX  produced by scripts/matrix.sh on 2026-09-10, commit ddd31d9
# measures  abstraction@6e37257 asks@1352b48 cas@1f99bd1 config@6deb3b2 download@5b3c636 identity@b812381 job@b126e63 logging@cbf5c4c model@160dbc3 rights@fb5baa4 router@e07620f storage@e97f1c0 watch@0a0beff
# regenerate  sh scripts/matrix.sh   (--check refuses a grid the evidence no longer produces)
# scope     this tree only. It reads what is committed here and the transcripts
#           in docs/results/; it runs no implementation and no harness. Each
#           transcript it cites carries its own state line.

    PASS      evidence exists, was reached, and the implementation kept the rules
    FAIL      evidence exists and a rule broke
    UNPROVEN  could not be checked here — see the reason
    ABSENT    checked, and the thing is confirmed not there
    —         no implementation of this layer in this language

    Each cell also names the toolchain that reached its verdict, and ? where the
    record it came from named none. A verdict over a compiler nobody named is a
    claim about a compiler nobody named.

layer       go             python         cpp            
----------- -------------  -------------  -------------  
abstraction UNPROVEN       —              —              
asks        UNPROVEN       —              —              
cas         UNPROVEN       UNPROVEN       UNPROVEN       
config      PASS go1.26.5  PASS py3.12    —              
download    UNPROVEN       UNPROVEN       UNPROVEN       
identity    UNPROVEN       —              —              
job         UNPROVEN       UNPROVEN       UNPROVEN       
logging     UNPROVEN       —              —              
model       UNPROVEN       UNPROVEN       —              
rights      UNPROVEN       —              —              
router      UNPROVEN       —              —              
storage     PASS go1.26.5  —              PASS g++/msvc  
watch       PASS go1.26.5  PASS py3.12    PASS g++/msvc  

39 cells over 13 layers: 7 PASS, 0 FAIL, 17 UNPROVEN, 0 ABSENT, 15 with no implementation.
24 implementations exist and 17 of them carry no verdict attributed to this tree.

why each cell reads as it does
  abstraction go      UNPROVEN       drifted     GATE recorded PASS at abstraction@139e861; the tree is abstraction@6e37257
  abstraction python  —              none        no python implementation, and the inventory declares none, so the gap is deliberate
  abstraction cpp     —              none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  asks        go      UNPROVEN       drifted     GATE recorded PASS at asks@8391834; the tree is asks@1352b48
  asks        python  —              none        no python implementation, and the inventory declares none, so the gap is deliberate
  asks        cpp     —              none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  cas         go      UNPROVEN       drifted     CAS-MIXED1 recorded PASS at cas@7565469; the tree is cas@1f99bd1
  cas         python  UNPROVEN       drifted     CAS-MIXED1 recorded PASS at cas@7565469; the tree is cas@1f99bd1
  cas         cpp     UNPROVEN       drifted     CAS-MIXED1 recorded PASS at cas@7565469; the tree is cas@1f99bd1
  config      go      PASS go1.26.5  recorded    GATE full, go build ./... && go test ./...
  config      python  PASS py3.12    recorded    GATE full, unittest discover, 17 tests
  config      cpp     —              none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  download    go      UNPROVEN       drifted     BEHAVIOUR1 recorded PASS at download@88561a4; the tree is download@5b3c636
  download    python  UNPROVEN       drifted     BEHAVIOUR1 recorded PASS at download@88561a4; the tree is download@5b3c636
  download    cpp     UNPROVEN       drifted     BEHAVIOUR1 recorded PASS at download@88561a4; the tree is download@5b3c636
  identity    go      UNPROVEN       drifted     GATE recorded PASS at identity@d9d7267; the tree is identity@b812381
  identity    python  —              none        no python implementation, and the inventory declares none, so the gap is deliberate
  identity    cpp     —              none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  job         go      UNPROVEN       drifted     AWAKE1 recorded PASS at job@5806dfe; the tree is job@b126e63
  job         python  UNPROVEN       drifted     BEHAVIOUR1 recorded PASS at job@5806dfe; the tree is job@b126e63
  job         cpp     UNPROVEN       drifted     BEHAVIOUR1 recorded PASS at job@5806dfe; the tree is job@b126e63
  logging     go      UNPROVEN       drifted     GATE recorded PASS at logging@68d0a1c; the tree is logging@cbf5c4c
  logging     python  —              none        no python implementation, and the inventory declares none, so the gap is deliberate
  logging     cpp     —              none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  model       go      UNPROVEN       drifted     GATE recorded PASS at model@0037120; the tree is model@160dbc3
  model       python  UNPROVEN       drifted     GATE recorded PASS at model@0037120; the tree is model@160dbc3
  model       cpp     —              none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  rights      go      UNPROVEN       drifted     GATE recorded PASS at rights@6d9d16d; the tree is rights@fb5baa4
  rights      python  —              none        no python implementation, and the inventory declares none, so the gap is deliberate
  rights      cpp     —              none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  router      go      UNPROVEN       drifted     GATE recorded PASS at router@0bdb1d8; the tree is router@e07620f
  router      python  —              undeclared  no python implementation, and the inventory carries no row for router, so nothing says whether the gap is deliberate
  router      cpp     —              undeclared  no cpp implementation, and the inventory carries no row for router, so nothing says whether the gap is deliberate
  storage     go      PASS go1.26.5  recorded    GATE full, go build ./... && go test ./...
  storage     python  —              none        no python implementation, and the inventory declares none, so the gap is deliberate
  storage     cpp     PASS g++/msvc  recorded    GATE full, ctest under g++ 15.2.0 in WSL + msvc 14.51.36231, 2 test runs
  watch       go      PASS go1.26.5  recorded    GATE full, go build ./... && go test ./...
  watch       python  PASS py3.12    recorded    GATE full, unittest discover, 19 tests
  watch       cpp     PASS g++/msvc  recorded    GATE full, ctest under g++ 15.2.0 in WSL + msvc 14.51.36231, 2 test runs

reasons
  recorded    a transcript in docs/results/ recorded this verdict for this tree
  drifted     a transcript recorded a verdict for a tree this one is no longer
  skipped     the last recorded gate run reached this cell and could not judge it
  gate        the gate runs this cell every time and no recorded run wrote down the result
  untested    the cell has no test file of its own
  unreached   nothing the gate runs names this cell
  declared    the inventory declares the language and the tree does not have it
  none        no implementation, and the inventory declares none, so the gap is deliberate
  undeclared  no implementation, and the inventory carries no row for the layer at all

the inventory declares layer directories this walk did not find: vault
