# MATRIX  produced by scripts/matrix.sh on 2026-09-11, commit 5e899ba
# measures  openabstractions-flat/abstraction-asks@7e9ea82 openabstractions-flat/abstraction-cas@f9cb388 openabstractions-flat/abstraction-config@9c4cbe5 openabstractions-flat/abstraction-download@43ab3b5 openabstractions-flat/abstraction-facade@4fee870 openabstractions-flat/abstraction-identity@b812381 openabstractions-flat/abstraction-job@30ffc73 openabstractions-flat/abstraction-logging@ec56256 openabstractions-flat/abstraction-model@7cf857e openabstractions-flat/abstraction-rights@6fff1f5 openabstractions-flat/abstraction-router@a56ecb2 openabstractions-flat/abstraction-storage@e97f1c0 openabstractions-flat/abstraction-watch@27bd982
# regenerate  sh scripts/matrix.sh   (--check refuses a grid the evidence no longer produces)
# scope     this tree only. It reads what is committed here and the transcripts
#           in docs/results/; it runs no implementation and no harness. Each
#           transcript it cites carries its own state line.

    PASS      the recorded checks passed within their stated scope and exceptions
    FAIL      evidence exists and a rule broke
    UNPROVEN  could not be checked here — see the reason
    ABSENT    checked, and the thing is confirmed not there
    —         no implementation of this layer in this language

    Each cell also names the toolchain that reached its verdict, and ? where the
    record it came from named none. A verdict over a compiler nobody named is a
    claim about a compiler nobody named.

layer                go                        python                        cpp                        
-------------------- -------------------------------  -------------------------------  -------------------------------  
abstraction-asks     FAIL* go1.26.5 darwin/arm64 cgo  —                        —                        
abstraction-cas      PASS go1.26.5                    PASS py3.12                      PASS Apple Clang darwin/arm64    
abstraction-config   FAIL go1.26.5 darwin/arm64 cgo   PASS py3.12                      —                        
abstraction-download FAIL* go1.26.5 darwin/arm64 cgo  PASS py3.12                      PASS Apple Clang darwin/arm64    
abstraction-facade   PASS go1.26.5                    —                        —                        
abstraction-identity PASS go1.26.5 darwin/arm64 cgo   —                        —                        
abstraction-job      FAIL go1.26.5 windows/amd64      FAIL CPython 3.12.14 win32       FAIL msvc 19.51.36256            
abstraction-logging  PASS go1.26.5                    —                        —                        
abstraction-model    PASS go1.26.5                    PASS py3.12                      —                        
abstraction-rights   FAIL* go1.26.5 darwin/arm64 cgo  —                        —                        
abstraction-router   PASS go1.26.5                    —                        —                        
abstraction-storage  PASS go1.26.5                    —                        PASS Apple Clang darwin/arm64    
abstraction-watch    PASS go1.26.5                    PASS py3.12                      PASS Apple Clang darwin/arm64    

39 cells over 13 layers: 17 PASS, 7 FAIL, 0 UNPROVEN, 0 ABSENT, 15 with no implementation.
24 implementations exist and 0 of them carry no verdict attributed to this tree.
* Documented platform limitation: the current macOS IPC provider cannot supply the process proof required by these service tests. These cells remain counted as FAIL.

why each cell reads as it does
  abstraction-asks     go      FAIL* go1.26.5 darwin/arm64 cgo  recorded    DARWIN1 , go test -count=1 ./... on native macOS; platform limitation: darwin-peer-proof
  abstraction-asks     python  —                        none        no python implementation, and the inventory declares none, so the gap is deliberate
  abstraction-asks     cpp     —                        none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  abstraction-cas      go      PASS go1.26.5                    recorded    GATE full, go build ./... && go test ./...
  abstraction-cas      python  PASS py3.12                      recorded    GATE full, unittest discover, 13 tests
  abstraction-cas      cpp     PASS Apple Clang darwin/arm64    recorded    DARWIN1 , CMake build and aggregate CTest on native macOS
  abstraction-config   go      FAIL go1.26.5 darwin/arm64 cgo   recorded    DARWIN1 , go test -count=1 ./... on native macOS
  abstraction-config   python  PASS py3.12                      recorded    GATE full, unittest discover, 17 tests
  abstraction-config   cpp     —                        none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  abstraction-download go      FAIL* go1.26.5 darwin/arm64 cgo  recorded    DARWIN1 , go test -count=1 ./... on native macOS; platform limitation: darwin-peer-proof
  abstraction-download python  PASS py3.12                      recorded    GATE full, unittest discover, 70 tests
  abstraction-download cpp     PASS Apple Clang darwin/arm64    recorded    DARWIN1 , CMake build and aggregate CTest on native macOS
  abstraction-facade   go      PASS go1.26.5                    recorded    GATE full, go build ./... && go test ./...
  abstraction-facade   python  —                        none        no python implementation, and the inventory declares none, so the gap is deliberate
  abstraction-facade   cpp     —                        none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  abstraction-identity go      PASS go1.26.5 darwin/arm64 cgo   recorded    DARWIN1 , go test -count=1 ./... on native macOS
  abstraction-identity python  —                        none        no python implementation, and the inventory declares none, so the gap is deliberate
  abstraction-identity cpp     —                        none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  abstraction-job      go      FAIL go1.26.5 windows/amd64      recorded    GATE full, the corpus: 16 disagreements with 120 fixtures that nothing records, 0 recorded and gone
  abstraction-job      python  FAIL CPython 3.12.14 win32       recorded    GATE full, the corpus: 16 disagreements with 120 fixtures that nothing records, 0 recorded and gone
  abstraction-job      cpp     FAIL msvc 19.51.36256            recorded    GATE full, the corpus: 11 disagreements with 120 fixtures that nothing records, 3 recorded and gone
  abstraction-logging  go      PASS go1.26.5                    recorded    GATE full, go build ./... && go test ./...
  abstraction-logging  python  —                        none        no python implementation, and the inventory declares none, so the gap is deliberate
  abstraction-logging  cpp     —                        none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  abstraction-model    go      PASS go1.26.5                    recorded    GATE full, go build ./... && go test ./...
  abstraction-model    python  PASS py3.12                      recorded    GATE full, unittest discover, 41 tests
  abstraction-model    cpp     —                        none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  abstraction-rights   go      FAIL* go1.26.5 darwin/arm64 cgo  recorded    DARWIN1 , go test -count=1 ./... on native macOS; platform limitation: darwin-peer-proof
  abstraction-rights   python  —                        none        no python implementation, and the inventory declares none, so the gap is deliberate
  abstraction-rights   cpp     —                        none        no cpp implementation, and the inventory declares none, so the gap is deliberate
  abstraction-router   go      PASS go1.26.5                    recorded    GATE full, go build ./... && go test ./...
  abstraction-router   python  —                        undeclared  no python implementation, and the inventory carries no row for openabstractions-flat/abstraction-router, so nothing says whether the gap is deliberate
  abstraction-router   cpp     —                        undeclared  no cpp implementation, and the inventory carries no row for openabstractions-flat/abstraction-router, so nothing says whether the gap is deliberate
  abstraction-storage  go      PASS go1.26.5                    recorded    GATE full, go build ./... && go test ./...
  abstraction-storage  python  —                        none        no python implementation, and the inventory declares none, so the gap is deliberate
  abstraction-storage  cpp     PASS Apple Clang darwin/arm64    recorded    DARWIN1 , CMake build and aggregate CTest on native macOS
  abstraction-watch    go      PASS go1.26.5                    recorded    GATE full, go build ./... && go test ./...
  abstraction-watch    python  PASS py3.12                      recorded    GATE full, unittest discover, 19 tests
  abstraction-watch    cpp     PASS Apple Clang darwin/arm64    recorded    DARWIN1 , CMake build and aggregate CTest on native macOS

reasons
  recorded    a transcript in docs/results/ recorded this verdict for this tree
  drifted     a transcript recorded a verdict for a tree this one is no longer
  skipped     the last recorded gate run reached this cell and could not judge it
  gate        the gate runs this cell every time and no recorded run wrote down the result
  untested    the cell has no test file of its own
  unreached   nothing the gate runs names this cell
  declared    the inventory declares the language and the tree does not have it
  none        no implementation, and the inventory declares none, so the gap is deliberate
  undeclared  no implementation, and the inventory carries no row for the layer at all
