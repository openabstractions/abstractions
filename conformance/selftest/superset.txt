# requires: store
#
# The four answers a weaker comparison used to accept. The driver returns a
# longer orphan list than the scenario names, a record where the scenario named
# only the verdict, a refusal whose word sits inside a phrase that denies it,
# and the right list in the wrong order. Each of these is a certificate this
# suite must not issue.
orphans
state A
say conflict
orphans

# expect 1: ok A
# expect 2: ok
# expect 3: conflict
# expect 4: ok C B A
