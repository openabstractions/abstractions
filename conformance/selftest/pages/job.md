# A stand-in for the job contract page

Not the contract. One rule, spelt the way the real page spells one, so the
selftest can show the same run reaching a verdict once the page it cites is
present. The real page is fetched by the command in `contracts.list`.

A write made once the lease has lapsed is refused [JOB-L5].
