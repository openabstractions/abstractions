cmake_minimum_required(VERSION 3.16)
project(config_observer_consumer LANGUAGES CXX)
find_package(abstraction_facade_resolution CONFIG REQUIRED)
find_package(abstraction_config CONFIG REQUIRED)
add_executable(config_observer_consumer main.cpp)
target_link_libraries(config_observer_consumer PRIVATE abstraction::facade_resolution abstraction::config_client)
