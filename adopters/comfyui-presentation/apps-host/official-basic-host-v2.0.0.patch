diff --git a/examples/basic-host/serve.ts b/examples/basic-host/serve.ts
index bc3fbff..7656612 100644
--- a/examples/basic-host/serve.ts
+++ b/examples/basic-host/serve.ts
@@ -15,2 +15,2 @@ import cors from "cors";
-import { fileURLToPath } from "url";
-import { dirname, join } from "path";
+import { readFileSync } from "fs";
+import { join } from "path";
@@ -19,2 +19 @@ import type { McpUiResourceCsp } from "@modelcontextprotocol/ext-apps";
-const __filename = fileURLToPath(import.meta.url);
-const __dirname = dirname(__filename);
+const __dirname = import.meta.dirname;
@@ -24,0 +24 @@ const DIRECTORY = join(__dirname, "dist");
+const SANDBOX_HTML = readFileSync(join(DIRECTORY, "sandbox.html"));
@@ -127 +127 @@ sandboxApp.get(["/", "/sandbox.html"], (req, res) => {
-  res.sendFile(join(DIRECTORY, "sandbox.html"));
+  res.type("html").send(SANDBOX_HTML);
diff --git a/examples/basic-host/src/implementation.ts b/examples/basic-host/src/implementation.ts
index 5f680ff..74f2f70 100644
--- a/examples/basic-host/src/implementation.ts
+++ b/examples/basic-host/src/implementation.ts
@@ -15,0 +16 @@ const IMPLEMENTATION = { name: "MCP Apps Host", version: "1.0.0" };
+const EXTENSION_ID = "io.modelcontextprotocol/ui";
@@ -55 +56 @@ async function connectWithFallback(serverUrl: URL): Promise<Client> {
-    const client = new Client(IMPLEMENTATION);
+    const client = new Client(IMPLEMENTATION, { capabilities: { extensions: { [EXTENSION_ID]: { mimeTypes: [RESOURCE_MIME_TYPE] } } } });
@@ -65 +66 @@ async function connectWithFallback(serverUrl: URL): Promise<Client> {
-    const client = new Client(IMPLEMENTATION);
+    const client = new Client(IMPLEMENTATION, { capabilities: { extensions: { [EXTENSION_ID]: { mimeTypes: [RESOURCE_MIME_TYPE] } } } });
