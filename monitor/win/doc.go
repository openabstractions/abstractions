// Package win is the window this project draws on the desktop: the platform's
// own window class, controls and message loop, reached through syscall.
//
// Only the Windows build has anything in it. Everywhere else the control panel
// is the page.
package win
